import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard",
};

const Page = () => {
  return (
    <div>
      <h2>List of workspaces here</h2>
    </div>
  );
};

export default Page;
